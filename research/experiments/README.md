# Experiment Specifications

Create one Markdown specification and one immutable config directory per study.
Each specification should state the hypothesis, treatment, controls, metrics,
seed set, compute budget, checkpoint schedule, and exclusion criteria.

A result is paper-eligible only when its final saved `config.json` agrees with the
committed specification and all compared methods use the same evaluation protocol.

