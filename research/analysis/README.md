# Analysis

Keep scripts for metric extraction, aggregation, statistical tests, figures, and
tables here. Generated artifacts belong under an ignored `artifacts/` directory;
the scripts and compact source data needed to regenerate them should be committed.

Report individual seeds alongside aggregate uncertainty, and distinguish the
predefined primary endpoint from post-hoc diagnostics.

